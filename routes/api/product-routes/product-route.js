const express=require('express');const base=require('../../../controller/product-controllers/product-controller');const writes=require('../../../controller/product-controllers/product-write-controller');const {getAllProductsForAdmin,getProductByIdForAdmin}=require('../../../controller/product-controllers/admin-catalog-controller');const {protect,restrictTo}=require('../../../middleware/auth-middleware');const {validate}=require('../../../middleware/validate');const {validateParam}=require('../../../middleware/validate-param');const {uploadImage}=require('../../../middleware/upload-image');const {productIdSchema,createProductSchema,editProductSchema}=require('../../../validation/product-validations/product-validation');const catalogImages=require('../../../controller/product-controllers/catalog-images-controller');const Joi=require('joi');const router=express.Router();
router.post('/:productId/gallery',protect,restrictTo('admin'),validateParam('productId',productIdSchema),uploadImage.array('images',10),catalogImages.uploadCatalogImages('Product'));router.put('/:productId/gallery',protect,restrictTo('admin'),validateParam('productId',productIdSchema),validate(Joi.object({images:Joi.array().max(10).unique().items(Joi.string()).required(),coverImage:Joi.string()}).unknown(false)),catalogImages.editCatalogImages('Product'));
router.get('/all',protect,restrictTo('admin'),getAllProductsForAdmin);router.get('/admin/:productId',protect,restrictTo('admin'),validateParam('productId',productIdSchema),getProductByIdForAdmin);router.get('/',base.getAllProducts);router.get('/id/:productId',validateParam('productId',productIdSchema),base.getProductById);router.get('/slug/:slug',base.getProductBySlug);router.post('/',protect,restrictTo('admin'),validate(createProductSchema),writes.addProduct);router.patch('/edit-cover/:productId',protect,restrictTo('admin'),validateParam('productId',productIdSchema),uploadImage.single('coverImage'),base.updateProductCover);router.delete('/delete-cover/:productId',protect,restrictTo('admin'),validateParam('productId',productIdSchema),base.deleteProductCover);router.post('/images/upload/:productId',protect,restrictTo('admin'),validateParam('productId',productIdSchema),uploadImage.array('images',10),base.uploadProductImages);router.put('/images/:productId',protect,restrictTo('admin'),validateParam('productId',productIdSchema),uploadImage.array('images',10),base.replaceProductImages);router.patch('/:productId',protect,restrictTo('admin'),validateParam('productId',productIdSchema),validate(editProductSchema),writes.editProductById);router.delete('/:productId',protect,restrictTo('admin'),validateParam('productId',productIdSchema),base.deleteProductById);module.exports=router;
